{erl_opts, [debug_info]}.
{deps, []}.

{escript_main_app, seagrass}.
{escript_name, sgc}.
{escript_emu_args, "%%! -escript main sg_cli\n"}.

{provider_hooks, [
    {post, [{compile, escriptize}]}
]}.
